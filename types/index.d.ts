type Product = {
    _id: string;
    name: string;
    price: string;
    quantity: string;
    colors: [string];
    sizes: [string];
    description: string;
    images: [string];
    category: string;
    gender: string;
};